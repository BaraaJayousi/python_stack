<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body style="display:flex; gap:10px; width: 1200px; margin: 50px auto; flex-wrap:wrap;" >
    {%for i in range(0, loops):%}
        <div style="height: 100px; width:100px; background-color: {{color}}; text-align:center; ">{{i+1}}</div>
    
    {%endfor%}
</body>
</html>